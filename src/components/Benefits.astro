---
const benefits = [
    {
        title: "Beneficios financieros",
        text: "Nuestro enfoque está en transformar la manera en que operas, reduciendo tiempos y errores para que obtengas mejores resultados a la vez que te liberamos de las tareas repetitivas que no aportan valor real a tu negocio. La inteligencia artificial será nuestro aliado, con ello optimizamos tus procesos y reducimos costos, lo que te permite aumentar tus márgenes y mejorar tus utilidades sin necesidad de incrementar precios."
    },
    {
        title: "Beneficios de eficiencia",
        text: "Automatizamos tareas repetitivas y optimizamos procesos clave para que tu equipo pueda enfocarse en lo estratégico y en la innovación. La inteligencia artificial se convierte en tu aliado, ejecutando operaciones rutinarias con una precisión constante y sin interrupciones. Esto no solo acelera tu productividad, sino que elimina cuellos de botella y garantiza que cada tarea se realice de manera eficiente y consistente."
    },
    {
        title: "Beneficios competitivos",
        text: "Te ayudamos a convertirte en una empresa ágil y competitiva. Al reducir costos y optimizar recursos, podrás ofrecer precios atractivos sin comprometer tus utilidades. Pero no solo se trata de precios: también podrás brindar una experiencia superior a tus clientes, posicionándote como la opción preferida en tu mercado. Serás esa empresa que marca la pauta, ofreciendo calidad y valor que nadie más puede igualar."
    },
    {
        title: "Beneficios para tus clientes",
        text: "Tus clientes no solo obtendrán un servicio eficiente, sino una experiencia diseñada para ellos. Personalizaremos cada interacción, ofreciendo soluciones rápidas, precisas y disponibles en todo momento. Esto no solo aumenta su satisfacción, sino que también fortalece la relación con tu marca, convirtiéndolos en defensores de tus productos o servicios. Cuando tus clientes son felices, tu negocio crece."
    }
];
---

<section class="benefits-section" id="beneficios">
    <div class="carousel-container">
        <div class="carousel">
            {benefits.map((benefit, index) => (
                <article class="benefit-card" data-index={index}>
                    <div class="card-content">
                        <h3>{benefit.title}</h3>
                        <p>{benefit.text}</p>
                    </div>
                </article>
            ))}
        </div>
        <div class="nav-controls">
            <button class="nav-button prev" aria-label="Previous benefit">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                    <path d="M15 18l-6-6 6-6" />
                </svg>
            </button>
            <div class="progress-dots">
                {benefits.map((_, index) => (
                    <button class="dot" data-index={index} aria-label={`Go to benefit ${index + 1}`}></button>
                ))}
            </div>
            <button class="nav-button next" aria-label="Next benefit">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                    <path d="M9 18l6-6-6-6" />
                </svg>
            </button>
        </div>
    </div>
    <a href="#contacto" class="contact-button">Contáctanos</a>
</section>

<style>
    .benefits-section {
        min-height: 100vh;
        padding: 0;
        background: #ffffff;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        margin-top: -8rem;
        padding-bottom: 6rem;
    }

    .carousel-container {
        width: 100%;
        max-width: 800px;
        position: relative;
        margin: 0 auto;
        padding-top: 2rem;
    }

    .carousel {
        position: relative;
        height: 400px;
        overflow: hidden;
    }

    .benefit-card {
        position: absolute;
        width: 100%;
        height: 100%;
        background: white;
        border: 1px solid rgba(0, 0, 0, 0.05);
        box-shadow: 20px 20px 60px rgba(0, 0, 0, 0.03);
        opacity: 0;
        transform: translateX(100%);
        transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .card-content {
        padding: 2rem 2rem;
        height: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
    }

    .benefit-card h3 {
        font-size: 1.8rem;
        font-weight: 500;
        margin: 0 0 2.5rem 0;
        color: #1a1a1a;
        text-align: center;
        width: 100%;
    }

    .benefit-card p {
        font-size: 1.1rem;
        line-height: 1.8;
        color: #4a4a4a;
        margin: 0 auto;
        max-width: 50ch;
        text-align: justify;
        text-justify: inter-word;
        hyphens: none;
        word-break: normal;
    }

    .benefit-card.active {
        opacity: 1;
        transform: translateX(0);
    }

    .benefit-card.previous {
        transform: translateX(-100%);
        opacity: 0;
    }

    .nav-controls {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-top: 1.5rem;
        padding: 0 1rem;
    }

    .nav-button {
        background: none;
        border: 1px solid rgba(0, 0, 0, 0.1);
        width: 40px;
        height: 40px;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #1a1a1a;
        transition: all 0.3s ease;
    }

    .nav-button:hover {
        background: #1a1a1a;
        color: white;
    }

    .progress-dots {
        display: flex;
        gap: 0.5rem;
    }

    .dot {
        width: 24px;
        height: 2px;
        border: none;
        background: #1a1a1a;
        opacity: 0.2;
        cursor: pointer;
        transition: all 0.3s ease;
        padding: 0;
    }

    .dot.active {
        opacity: 1;
        width: 32px;
    }

    .contact-button {
        font-size: 1.8rem;
        font-weight: 400;
        letter-spacing: 0.1em;
        color: #1a1a1a;
        text-decoration: none;
        padding: 1rem 2rem;
        margin-top: 1rem;
        margin-bottom: 5rem;
        position: relative;
        bottom: 0;
    }

    .contact-button::after {
        content: '';
        position: absolute;
        width: 100%;
        height: 1px;
        bottom: 0;
        left: 0;
        background-color: currentColor;
        transform: scaleX(0);
        transform-origin: right;
        transition: transform 0.6s cubic-bezier(0.19, 1, 0.22, 1);
    }

    .contact-button:hover::after {
        transform: scaleX(1);
        transform-origin: left;
    }

    @media (max-width: 768px) {
        .card-content {
            padding: 2rem;
        }

        .benefit-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1.5rem;
        }

        .benefit-card p {
            font-size: 1rem;
            line-height: 1.6;
        }
    }
</style>

<script>
    let currentIndex = 0;
    const cards = document.querySelectorAll('.benefit-card');
    const dots = document.querySelectorAll('.dot');
    const totalCards = cards.length;

    function updateCards() {
        cards.forEach((card, index) => {
            card.classList.remove('active', 'previous');
            if (index === currentIndex) {
                card.classList.add('active');
            } else if (index === (currentIndex - 1 + totalCards) % totalCards) {
                card.classList.add('previous');
            }
        });

        dots.forEach((dot, index) => {
            dot.classList.toggle('active', index === currentIndex);
        });
    }

    function nextCard() {
        currentIndex = (currentIndex + 1) % totalCards;
        updateCards();
    }

    function prevCard() {
        currentIndex = (currentIndex - 1 + totalCards) % totalCards;
        updateCards();
    }

    // Initialize first card
    cards[0].classList.add('active');
    dots[0].classList.add('active');

    // Event listeners
    document.querySelector('.next').addEventListener('click', nextCard);
    document.querySelector('.prev').addEventListener('click', prevCard);

    dots.forEach((dot, index) => {
        dot.addEventListener('click', () => {
            currentIndex = index;
            updateCards();
        });
    });

    // Touch/Swipe support
    let touchStartX = 0;
    const carousel = document.querySelector('.carousel');

    carousel.addEventListener('touchstart', e => {
        touchStartX = e.changedTouches[0].screenX;
    }, { passive: true });

    carousel.addEventListener('touchend', e => {
        const touchEndX = e.changedTouches[0].screenX;
        const diff = touchStartX - touchEndX;

        if (Math.abs(diff) > 50) {
            if (diff > 0) {
                nextCard();
            } else {
                prevCard();
            }
        }
    }, { passive: true });
</script>
