---
---

<section class="hero-section" id="hero">
    <div class="hero-content">
        <p class="text">
           Te ayudamos a construir un negocio inteligente orientado a la integración de IA, innovación y automatización.
        </p>
        <a href="#beneficios" class="contact-button">
            Conoce los beneficios
        </a>
    </div>
</section>

<style>
    .hero-section {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
        margin-top: -5vh;
    }

    .hero-content {
        max-width: 800px;
        width: 90%;
        margin: 0 auto;
        text-align: center;
    }

    .text {
        font-size: clamp(2.5rem, 4vw, 3.5rem);
        font-weight: 400;
        line-height: 1.2;
        color: #1a1a1a;
        margin: 0;
        margin-bottom: 6rem;
    }

    .contact-button {
        font-size: 1.8rem;
        font-weight: 400;
        letter-spacing: 0.1em;
        color: #1a1a1a;
        text-decoration: none;
        padding: 0.4rem 0;
        position: relative;
    }

    .contact-button::after {
        content: '';
        position: absolute;
        width: 100%;
        height: 1px;
        bottom: 0;
        left: 0;
        background-color: currentColor;
        transform: scaleX(0);
        transform-origin: right;
        transition: transform 0.6s cubic-bezier(0.19, 1, 0.22, 1);
    }

    .contact-button:hover::after {
        transform: scaleX(1);
        transform-origin: left;
    }

    @media (max-width: 768px) {
        .hero-section {
            padding: 1rem;
            margin-top: -10vh;
            min-height: 100vh;
            height: 100vh;
        }

        .hero-content {
            width: 95%;
        }

        .text {
            max-width: none;
        }

        .contact-button {
            font-size: 1.4rem;
        }
    }
</style>
